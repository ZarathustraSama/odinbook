<h1>New Post</h1>
<%= form_with model: @post do |f| %>
  <%= f.text_field :image_url, placeholder: "Image URL (Optional)" %>
  <br>
  <%= f.text_area :body, size: "50x10" %>
  <%= f.submit "Post" %>
<% end %>
